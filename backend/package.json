{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "Sette0o0",
  "type": "module",
  "scripts": {
    "prisma:generate": "npx prisma migrate dev --name init && npx prisma generate",
    "prisma:seed": "npx prisma db seed",
    "build": "npm run prisma:generate && tsc",
    "run": "node dist/src/server.js",
    "start:completo": "npm run build && npm run prisma:seed && npm run run",
    "start": "tsc && npm run run"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^25.9.2",
    "prisma": "^7.8.0",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@prisma/adapter-better-sqlite3": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  }
}
